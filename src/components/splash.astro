---
import { Image } from "astro:assets";
import matiaHero from "~/assets/matia-hero.png"; // ضع صورة بانر رئيسية (ديكور الصالة/الخدمات)

const widths = [600, 900, 1200];
const sizes = "(min-width: 1024px) 40vw, 80vw";

const PHONE = "+966550140714";
const WHATSAPP = "https://wa.me/966550140714";
const MAPS =
  "https://www.google.com/maps/search/?api=1&query=R42V%2BF56%2C%20Jeddah%2023832";
---

<section class="relative h-full bg-[var(--color-background)]">
  <div class="relative grid h-full place-items-center gap-10 sm:grid-cols-2">
    <!-- نص البطل -->
    <div class="order-2 space-y-4 text-center sm:order-1 sm:text-right">
      <h2
        class="gradient-text font-extrabold leading-tight tracking-tight text-5xl md:text-7xl"
      >
        استرخاءٌ فاخر
        <br />
        عناية متكاملة
      </h2>
      <p
        class="mx-auto max-w-xl text-[var(--color-text-offset)] text-lg md:text-xl"
      >
        ماتيا سبا النسائي في جدة — عناية بالأظافر، والجسم، والبشرة، والشعر، مع
        منتجات تجميلية طبيعية.
      </p>
      <div
        class="flex flex-wrap items-center justify-center gap-3 sm:justify-start"
      >
        <a
          href={`tel:${PHONE}`}
          class="rounded-full bg-[var(--color-primary)] px-6 py-3 font-medium text-white text-sm hover:opacity-95 focus:outline-none focus:ring"
        >
          اتصال الآن
        </a>
        <a
          href={WHATSAPP}
          target="_blank"
          rel="noopener"
          class="rounded-full border border-[var(--color-border)] bg-[var(--color-background-offset)] px-6 py-3 font-medium text-sm hover:opacity-95 focus:outline-none focus:ring"
        >
          حجز عبر واتساب
        </a>
        <a
          href={MAPS}
          target="_blank"
          rel="noopener"
          class="rounded-full px-6 py-3 font-medium text-sm hover:opacity-95 focus:outline-none focus:ring"
          style="background:transparent;color:var(--color-secondary)"
        >
          اتجهي إلينا
        </a>
      </div>

      <div class="mt-3 text-[var(--color-text-offset)] text-sm">
        <span
          class="inline-block rounded-full bg-[var(--color-background-offset)] px-3 py-1"
        >
          ⭐ 4.63 على Google
        </span>
        <span class="mx-2">•</span>
        <span>مفتوح اليوم حتى 11 مساءً</span>
      </div>
    </div>

    <!-- صورة البطل -->
    <div
      id="hero-media"
      class="order-1 w-11/12 max-w-3xl sm:order-2 sm:w-10/12"
    >
      <Image
        class="h-full w-full rounded-3xl object-cover shadow-2xl"
        src={matiaHero}
        widths={widths}
        sizes={sizes}
        loading="eager"
        alt="ماتيا سبا — أجواء هادئة وخدمات عناية"
      />
    </div>
  </div>
</section>

<style>
  /* حركة طفيفة للصورة لإحساس نابض بالحياة */
  @keyframes float {
    0% {
      transform: translate3d(0, 0, 0);
    }
    100% {
      transform: translate3d(0, 18px, 0);
    }
  }

  #hero-media {
    animation: float linear 3s infinite alternate;
  }

  @media (prefers-reduced-motion: reduce) {
    #hero-media {
      animation: none;
    }
  }
</style>
