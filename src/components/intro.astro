---
import { Icon } from "astro-icon/components";
import ContentSection from "~/components/content-section.astro";

const PHONE = "+966550140714";
const WHATSAPP = "https://wa.me/966550140714";
const MAPS =
  "https://www.google.com/maps/search/?api=1&query=R42V%2BF56%2C%20Jeddah%2023832";
const IG = "https://www.instagram.com/matia_csp/";
---

<ContentSection title="لماذا ماتيا؟" id="intro">
  <Icon
    slot="eyebrow"
    name="logomark"
    class="size-32 text-[var(--color-secondary)]"
  />
  <Fragment slot="lead">
    ماتيا سبا علامة سعودية مُسجَّلة للعناية المتكاملة —
    <span class="text-primary">الأظافر</span>،
    <span class="text-primary">الجسم</span>،
    <span class="text-primary">البشرة</span>، و<span class="text-primary"
      >الشعر</span
    > — بأجواءٍ فاخرة ومواد طبيعية.
  </Fragment>

  <div class="grid grid-cols-1 gap-2 sm:grid-cols-2">
    <a
      href="#features"
      class="flex items-center justify-center gap-3 border-2 border-current px-6 py-4"
      aria-label="تعرّفي على خدماتنا"
    >
      <Icon name="mdi:spa" class="size-8" />
      <span>تعرّفي على الخدمات</span>
    </a>
    <a
      href={WHATSAPP}
      target="_blank"
      rel="noopener"
      class="flex items-center justify-center gap-3 border-2 border-current px-6 py-4"
      aria-label="حجز عبر واتساب"
    >
      <Icon name="mdi:whatsapp" class="size-8" />
      <span>حجز عبر واتساب</span>
    </a>
  </div>

  <div class="mt-3 grid grid-cols-1 gap-2 sm:grid-cols-2">
    <a
      href={MAPS}
      target="_blank"
      rel="noopener"
      class="flex items-center justify-center gap-3 border-2 border-dashed border-current px-6 py-4"
      aria-label="اتجهي إلينا عبر الخرائط"
    >
      <Icon name="mdi:map-marker" class="size-8" />
      <span>اتجهي إلينا</span>
    </a>
    <a
      href={`tel:${PHONE}`}
      class="flex items-center justify-center gap-3 border-2 border-dashed border-current px-6 py-4"
      aria-label="اتصال"
    >
      <Icon name="mdi:phone" class="size-8" />
      <span>اتصال مباشر</span>
    </a>
  </div>
</ContentSection>
